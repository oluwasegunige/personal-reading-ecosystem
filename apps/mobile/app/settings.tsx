import { SafeAreaView, Text } from 'react-native';
import type { ReactElement } from 'react';

export default function Settings(): ReactElement {
  return (
    <SafeAreaView>
      <Text>Settings scaffold</Text>
    </SafeAreaView>
  );
}
