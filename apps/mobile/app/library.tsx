import { SafeAreaView, Text } from 'react-native';
import type { ReactElement } from 'react';

export default function Library(): ReactElement {
  return (
    <SafeAreaView>
      <Text>Library scaffold</Text>
    </SafeAreaView>
  );
}
